"""
app/__init__.py
The `app` package now contains only main.py.
All domain modules (core, api, schemas, services, etc.) live at the
backend root level — siblings of `app`, not children of it.
"""

__version__ = "0.1.0"
